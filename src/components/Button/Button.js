import React from "react";
import "./Button.css";

const Button = () => {
    return (
        <div className="neon__btn">
            <button></button>
        </div>
    );
};

export default Button;
